name: Stream Videos to YouTube

on:
  workflow_dispatch:

jobs:
  stream:
    runs-on: ubuntu-latest
    timeout-minutes: 355

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Install FFmpeg and gdown
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg python3-pip
          pip3 install -U gdown

      - name: Download videos
        run: |
          mkdir -p videos

          gdown --folder \
            "https://drive.google.com/drive/folders/1htbSr_XtI4XTTV_Cpq8Wypitv000SuTP?usp=sharing" \
            -O videos

          echo "Downloaded files:"
          ls -lh videos

      - name: Check videos
        run: |
          for i in $(seq 1 16); do
            if [ ! -f "videos/Video $i.mp4" ]; then
              echo "ERROR: Video $i.mp4 is missing!"
              exit 1
            fi
          done

          echo "All 16 videos found successfully."

      - name: Create playlist
        run: |
          rm -f playlist.txt

          for i in $(seq 1 16); do
            echo "file 'videos/Video $i.mp4'" >> playlist.txt
          done

          cat playlist.txt

      - name: Stream to YouTube
        env:
          STREAM_KEY: ${{ secrets.STREAM_KEY }}
        run: |
          ffmpeg \
            -re \
            -stream_loop -1 \
            -f concat \
            -safe 0 \
            -i playlist.txt \
            -vf "scale=1080:1920:force_original_aspect_ratio=decrease,pad=1080:1920:(ow-iw)/2:(oh-ih)/2,format=yuv420p" \
            -c:v libx264 \
            -preset veryfast \
            -b:v 6000k \
            -maxrate 6000k \
            -bufsize 12000k \
            -r 30 \
            -g 60 \
            -keyint_min 60 \
            -sc_threshold 0 \
            -pix_fmt yuv420p \
            -c:a aac \
            -b:a 128k \
            -ar 44100 \
            -ac 2 \
            -f flv \
            "rtmp://a.rtmp.youtube.com/live2/${STREAM_KEY}"
